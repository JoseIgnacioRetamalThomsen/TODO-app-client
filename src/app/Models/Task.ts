export class Task {

    constructor
        (
        public taskListId : string,
        public taskTitle: string,
        public taskBody: string,
        public isCompleted: boolean

        ) {

    }
}